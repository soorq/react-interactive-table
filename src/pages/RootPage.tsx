import * as React from 'react';
import { Header } from '@/widgets/Layout/Header';

export function RootPage() {
    return (
        <React.Fragment>
            <Header />
        </React.Fragment>
    );
}
